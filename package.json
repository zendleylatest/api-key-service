{
  "name": "openai-api-key-service",
  "version": "1.0.0",
  "private": true,
  "description": "Small service for reading and updating an OpenAI API key.",
  "main": "index.js",
  "scripts": {
    "start": "node --env-file=.env index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
